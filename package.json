{
  "name": "wowgoldtools-backend-elysia",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "start": "bun run --watch --expose-gc src/index.ts",
    "build": "bun build --compile --minify-whitespace --minify-syntax --target bun --outfile server ./src/index.ts prisma generate"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.2.0",
    "@prisma/client": "^6.4.1",
    "elysia": "latest",
    "p-limit": "^6.2.0"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prisma": "^6.4.1"
  },
  "module": "src/index.js"
}
